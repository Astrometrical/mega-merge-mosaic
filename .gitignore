# Rust
/target

# Test data — multi-GB astro panels, never committed
/test_data/

# MMM session caches (tiled panel cache, analysis artifacts)
*.mmm-session/
/sessions/

# Blend outputs in the repo root (runs during development)
/*.fits
/*.tiff
/*.png

# WSL / Windows artifacts
*Zone.Identifier

# Editor
.vscode/
.idea/
